const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const paths={box:'M12 3 3 8v9l9 5 9-5V8z M3 8l9 5 9-5 M12 13v9',grid:'M3 3h7v7H3z M14 3h7v7h-7z M3 14h7v7H3z M14 14h7v7h-7z',help:'M9 9a3 3 0 0 1 6 0c0 2-3 2-3 5 M12 18h.01 M22 12a10 10 0 1 1-20 0 10 10 0 0 1 20 0',search:'M21 21l-5-5 M18 10a8 8 0 1 1-16 0 8 8 0 0 1 16 0',rig:'M8 19 11 3h2l3 16 M9 14h6 M10 9h4 M4 19h16 M6 22v-3 M18 22v-3',shield:'M12 3 3 6v6c0 5 9 10 9 10s9-5 9-10V6z M8 12l3 3 5-6','chevron-left':'M15 5 8 12l7 7','chevron-right':'M9 5 16 12l-7 7','chevron-down':'M5 9 12 16l7-7',menu:'M4 6h16 M4 12h16 M4 18h16',panel:'M3 3h18v18H3z M15 3v18',focus:'M8 3H3v5 M16 3h5v5 M3 16v5h5 M21 16v5h-5 M8 12a4 4 0 1 1 8 0 4 4 0 0 1-8 0',layers:'m12 3 10 6-10 6L2 9z M2 13l10 6 10-6 M2 17l10 6 10-6','eye-off':'M3 3l18 18 M10 6l2-.1c7 0 10 6 10 6a19 19 0 0 1-4 4 M6 6c-3 2-5 6-5 6s4 6 11 6l3-.4 M9 9a4 4 0 0 0 6 6',cut:'M3 3h18v18H3z M3 12h18 M12 3v18 M6 6l3 3 M15 15l3 3',waves:'M2 6c3-4 6 4 10 0s6 4 10 0 M2 12c3-4 6 4 10 0s6 4 10 0 M2 18c3-4 6 4 10 0s6 4 10 0',reset:'M3 11a9 9 0 1 1 2 7 M3 4v7h7',home:'m3 10 9-7 9 7 M5 9v12h14V9 M9 21v-8h6v8',expand:'M9 3H3v6 M15 3h6v6 M3 15v6h6 M21 15v6h-6',close:'M6 6l12 12 M18 6 6 18',arrow:'M4 12h16 M14 6l6 6-6 6',hoist:'M8 3v14 M4 13l4 4 4-4 M16 21V7 M12 11l4-4 4 4',rotate:'M21 9a9 9 0 1 0 0 6 M21 3v6h-6',drop:'M12 2C9 7 4 12 4 16a8 8 0 0 0 16 0c0-4-5-9-8-14z',bolt:'m13 2-9 12h7l-1 8 10-13h-7z',pipe:'M9 2h6v20H9z M7 5h10 M7 19h10 M12 8v8',structure:'M4 3h16v18H4z M4 9h16 M4 15h16 M10 3v18 M16 3v18',anchor:'M12 8v14 M5 13H2a10 10 0 0 0 20 0h-3 M9 4a3 3 0 1 1 6 0 3 3 0 0 1-6 0',link:'M9 15l6-6 M10 6l3-3a5 5 0 0 1 7 7l-3 3 M14 18l-3 3a5 5 0 0 1-7-7l3-3',info:'M12 11v6 M12 7h.01 M22 12a10 10 0 1 1-20 0 10 10 0 0 1 20 0',download:'M12 3v12 M7 10l5 5 5-5 M3 16v5h18v-5'};
const icon=n=>`<svg viewBox="0 0 24 24" aria-hidden="true"><path d="${paths[n]||paths.box}"/></svg>`;
function icons(root=document){root.querySelectorAll('[data-icon]').forEach(el=>el.innerHTML=icon(el.dataset.icon));}icons();
const allSystems=[{id:'HS',name:'Hoisting',icon:'hoist'},{id:'RS',name:'Rotating',icon:'rotate'},{id:'CS',name:'Circulating',icon:'drop'},{id:'WC',name:'Well Control',icon:'shield'},{id:'PS',name:'Power & Electrical',icon:'bolt'},{id:'DS',name:'Drill String & BHA',icon:'pipe'},{id:'SS',name:'Structure & Drill Floor',icon:'structure'},{id:'JU',name:'Jacking & Cantilever',icon:'rig'},{id:'MR',name:'Hull & Marine',icon:'anchor'},{id:'HY',name:'Hydraulic Lift & Slips',icon:'hoist'}];
let systems=allSystems;
const tabs=['Overview','Engineering','Connections','Safety','Specs','Evidence'];
let mode='jackup',allComponents=[],components=[],catalog=null,hwuCatalog=null,hwuEquipment=null,rigEquipment=null,fleetAsset='jackup',sourceNote='',scene=null,selected=null,selectedRig=null,tab='Overview',view='explorer',fleetFilter='rig_record',fleetQuery='',expandAll=false,toastTimer;
const byId=new Map();
const modelInfo={
  jackup:{title:'Jack-up rig',overview:'Offshore jack-up',short:'Jack-up',eyebrow:'OFFSHORE ENGINEERING',code:'JU-01',environment:'Offshore · illustrative shallow water',type:'Independent-leg jack-up',description:'A self-elevating platform with a cantilevered drilling package.',intro:'Explore the systems that make a jack-up drilling rig work.',quick:[['top-drive','Top drive','rotate'],['bop','Well control','shield'],['jacking','Jacking system','rig']],sourceSection:'JACK-UP'},
  land:{title:'Land drilling rig',overview:'Land drilling rig',short:'Land rig',eyebrow:'ONSHORE ENGINEERING',code:'LR-01',environment:'Onshore · illustrative drill site',type:'Land drilling rig',description:'A land-based drilling package on a steel substructure.',intro:'Explore the systems that make a land drilling rig work.',quick:[['top-drive','Top drive','rotate'],['bop','Well control','shield'],['drill-floor','Drill floor','rig']],sourceSection:'JACK-UP'},
  hwu:{title:'Hydraulic workover unit',overview:'Hydraulic workover unit',short:'HWU',eyebrow:'WELL INTERVENTION',code:'HWU',environment:'Offshore · illustrative platform deck',type:'Platform-mounted hydraulic workover unit',description:'A compact hydraulic jack moves tubulars using stationary and travelling slip assemblies above surface well-control equipment. The supporting platform is illustrative context.',intro:'Explore the hydraulic lifting, tubular handling and pressure-control systems.',quick:[['hwu-jack','Hydraulic jacking assembly','hoist'],['hwu-bop','Pressure-control stack','shield'],['hwu-hpu','Hydraulic power pack','bolt']],sourceSection:'Work Over · HWU'}
};
const currentFleet=()=>fleetAsset==='hwu'?hwuCatalog:catalog;
const recordKind=()=>fleetAsset==='hwu'?'unit_record':'rig_record';
const isUnitRecord=r=>r.recordKind==='rig_record'||r.recordKind==='unit_record';
function filteredFleet(){return(currentFleet()?.rigs||[]).filter(r=>r.recordKind===fleetFilter&&[r.name,r.sourceLabel,...r.aliases,...r.planningLabels].join(' ').toLowerCase().includes(fleetQuery.toLowerCase()));}

function toast(message){$('#toast').textContent=message;$('#toast').hidden=false;clearTimeout(toastTimer);toastTimer=setTimeout(()=>$('#toast').hidden=true,3500);}
function dialog(title,body){$('#dialog-title').textContent=title;$('#dialog-body').innerHTML=body;$('#dialog').showModal();}
$('#dialog-close').onclick=()=>$('#dialog').close();$('#dialog').addEventListener('click',e=>{if(e.target===$('#dialog'))$('#dialog').close();});
function setView(next){view=next;$('#workspace').hidden=next!=='explorer';$('#fleet-view').hidden=next!=='fleet';$$('.nav-tab').forEach(b=>b.classList.toggle('active',b.dataset.view===next));if(next==='fleet')renderFleet();else scene?.resize();}
$$('[data-view]').forEach(b=>b.onclick=()=>setView(b.dataset.view));$('.brand').onclick=e=>{e.preventDefault();setView('explorer');runAction('reset');};$('#show-fleet').onclick=()=>setView('fleet');
function renderTree(){const query=$('#search').value.toLowerCase().trim();const previous=new Set($$('.system-item[open]').map(x=>x.dataset.system));let html='';for(const sys of systems){const all=components.filter(c=>c.system===sys.id);const items=all.filter(c=>[c.name,c.code,...c.aliases,sys.name].some(s=>s.toLowerCase().includes(query)));if(!items.length)continue;const open=query||expandAll||previous.has(sys.id)||(selected&&byId.get(selected).system===sys.id);html+=`<details class="system-item" data-system="${sys.id}" ${open?'open':''}><summary class="system-summary"><span class="sys-icon">${icon(sys.icon)}</span><span class="sys-copy"><b>${sys.name}</b><small>${sys.id} · ${all.length} ${all.length===1?'assembly':'assemblies'}</small></span>${icon('chevron-right')}</summary><div class="component-list">${items.map(c=>`<button class="component-btn ${selected===c.id?'active':''}" data-component="${c.id}" title="${esc(c.code)}">${icon('box')}<span>${esc(c.name)}</span></button>`).join('')}</div></details>`;}$('#equipment-tree').innerHTML=html||'<div class="empty-search">No equipment matches. Try a name or component ID.</div>';$$('.component-btn').forEach(b=>b.onclick=()=>select(b.dataset.component,true));}
$('#search').addEventListener('input',renderTree);$('#expand-all').onclick=()=>{expandAll=!expandAll;$$('.system-item').forEach(d=>d.open=expandAll);$('#expand-all').textContent=expandAll?'Collapse all':'Expand all';};
function select(id,frame=true){const c=byId.get(id);if(!c)return;selected=id;tab='Overview';scene?.select(id,frame);renderTree();renderInspector();syncButtons();$('#breadcrumbs').innerHTML=`${icon('home')}<span>${systems.find(s=>s.id===c.system)?.name}</span><span class="crumb-sep">/</span><b>${esc(c.name)}</b>`;if(innerWidth<=900){$('#workspace').classList.remove('right-collapsed');$('#workspace').classList.add('mobile-inspector');}if(innerWidth<=650)$('#workspace').classList.remove('mobile-navigator');}
function renderInspector(){const info=modelInfo[mode];const c=byId.get(selected);const panel=$('#inspector-content');if(!c){panel.innerHTML=`<div class="inspector-hero"><div class="inspector-kicker">${icon('rig')}RIG OVERVIEW</div><h2>${esc(selectedRig?.name||info.overview)}</h2><p>${info.intro}</p><div class="record-meta"><span class="badge">Illustrative model</span><code>MTR · ${info.code}</code></div></div><div class="inspector-body"><h3>Built around the well</h3><p>${info.description} Select equipment in the scene to explore its purpose, connections and internal assemblies.</p><div class="overview-metrics"><div class="metric"><strong>${components.length}</strong><span>Equipment assemblies</span></div><div class="metric"><strong>${systems.length}</strong><span>Connected systems</span></div></div><div class="info-tile"><label>ENVIRONMENT</label><b>${info.environment}</b></div><div class="info-tile"><label>MODEL TYPE</label><b>${info.type}</b></div><h3>Start exploring</h3>${info.quick.map(([id,label,symbol])=>`<button class="connection-btn" data-jump="${id}">${icon(symbol)}${label} ${icon('chevron-right')}</button>`).join('')}${selectedRig?`<h3>Selected LTDS record</h3><p>${esc(selectedRig.sourceLabel)}</p><p>${esc(selectedRig.note)}</p><span class="badge amber">Planning record · June 2026</span>`:''}<p class="caption-note">Generic training geometry. Actual rig layouts, equipment and dimensions are not verified. Master Team Resources Sdn Bhd · 1041387-W.</p></div>`;bindInspector();return;}
const sys=systems.find(s=>s.id===c.system);panel.innerHTML=`<div class="inspector-hero"><div class="inspector-kicker">${icon(sys.icon)}${sys.name} <span>·</span> ASSEMBLY</div><h2>${esc(c.name)}</h2><div class="record-meta"><span class="badge">Illustrative</span><code>${esc(c.code)}</code></div><div class="inspector-action"><button class="primary-btn" data-inspect-action="focus">${icon('focus')}Focus <kbd>F</kbd></button><button class="secondary-btn" data-inspect-action="isolate">${icon('box')}Isolate</button></div></div><div class="inspector-tabs" role="tablist" aria-label="Equipment details">${tabs.map(t=>`<button class="inspector-tab ${t===tab?'active':''}" role="tab" aria-selected="${t===tab}" data-tab="${t}">${t}</button>`).join('')}</div><div class="inspector-body" role="tabpanel" aria-label="${tab}">${inspectorBody(c)}</div>`;bindInspector();}
function inspectorBody(c){if(tab==='Overview')return`<h3>What it does</h3><p>${esc(c.summary)}</p><h3>Where to find it</h3><div class="info-tile"><label>LOCATION</label><b>${esc(c.location)}</b></div><h3>Connected to</h3>${c.connections.slice(0,3).map(id=>connectionButton(id)).join('')}<p class="caption-note">This is an illustrative assembly. Rig-specific configuration requires verified drawings and equipment records.</p>`;if(tab==='Engineering')return`<h3>How the assembly works</h3><p>${esc(c.engineering)}</p><h3>Model interpretation</h3><p>Geometry shows a generic arrangement and recognizable parts. It is not dimensioned engineering documentation.</p>${scene?.hasInternals(c.id)?`<button class="primary-btn" data-inspect-action="internals">${icon('cut')}Explore internal parts</button><p class="caption-note">Internal parts are illustrative. External housings become translucent.</p>`:'<div class="info-tile"><label>INTERNAL GEOMETRY</label><b>Not modelled for this assembly</b></div>'}`;if(tab==='Connections')return`<h3>Related equipment</h3><p>Conceptual physical or functional relationships in this training model.</p>${c.connections.map(connectionButton).join('')}<button class="primary-btn" data-inspect-action="connections">${icon('link')}${scene?.state.connections?'Hide':'Show'} connections in 3D</button><p class="caption-note">These are undirected illustrative relationships, not verified piping or wiring routes.</p>`;if(tab==='Safety')return`<h3>Hazard awareness</h3><ul>${c.hazards.map(h=>`<li>${esc(h)}</li>`).join('')}</ul><div class="info-tile"><label>BEFORE OPERATION</label><b>Use the rig's approved procedures and equipment documentation.</b></div><p class="caption-note">Educational awareness only. The model provides no operating sequence, certification or readiness assessment.</p>`;if(tab==='Specs')return`<h3>Record details</h3><div class="spec-row"><span>Component ID</span><b>${esc(c.code)}</b></div><div class="spec-row"><span>Model record</span><b>${esc(c.id)}</b></div><div class="spec-row"><span>Geometry</span><b>Illustrative</b></div><div class="spec-row"><span>Manufacturer</span><b>Not provided</b></div><div class="spec-row"><span>Rated capacity</span><b>Not provided</b></div><div class="spec-row"><span>Dimensions</span><b>Not verified</b></div><p class="caption-note">The LTDS planning document does not provide equipment specifications. Visual model proportions are not technical ratings.</p>`;return`<h3>Model evidence</h3><span class="badge amber">Generic · not rig-specific</span><p>${esc(sourceNote)}</p>${mode==='hwu'&&hwuEquipment.sources?.length?`<h3>Generic equipment references</h3>${hwuEquipment.sources.filter(ref=>!ref.componentIds||ref.componentIds.includes(c.id)).map(ref=>`<p><a class="evidence-link" href="${esc(ref.url)}" target="_blank" rel="noopener noreferrer">${esc(ref.title)}</a></p>`).join('')}`:''}<h3>Fleet planning source</h3><div class="info-tile"><label>JUNE 2026 · PAGE ${selectedRig?.sourcePage||1} · ${esc(modelInfo[mode].sourceSection)}</label><b>June 2026 LTDS planning attachment &middot; withheld from this edition</b></div><p>The attachment supports planning records, not this model's geometry or equipment specifications.</p><h3>Open information</h3><ul><li>Rig-specific equipment inventory</li><li>Verified arrangement drawings</li><li>Manufacturer ratings and manuals</li></ul><p class="caption-note">LTDS source classification: Confidential / Internal. Future schedule headings are for indication only.</p>`;}
function connectionButton(id){return`<button class="connection-btn" data-jump="${id}">${icon('link')}<span>${esc(byId.get(id)?.name||id)}</span></button>`;}
function bindInspector(){$$('[data-jump]').forEach(b=>b.onclick=()=>select(b.dataset.jump,true));$$('[data-tab]').forEach(b=>b.onclick=()=>{tab=b.dataset.tab;renderInspector();});$$('[data-inspect-action]').forEach(b=>b.onclick=()=>runAction(b.dataset.inspectAction));}
function syncButtons(){const state=scene?.state||{};$$('[data-action]').forEach(b=>{const action=b.dataset.action;const needs=['focus','isolate','fade','hide','internals'].includes(action);b.disabled=(needs&&!selected)||(action==='internals'&&!scene?.hasInternals(selected));b.classList.toggle('active',!!state[action]);});$$('.sys-chip').forEach(b=>b.classList.toggle('off',(state.hiddenSystems||[]).includes(b.dataset.systemToggle)));}
function runAction(action){if(!scene){toast('The 3D scene is still loading.');return;}const result=scene.action(action);if(result.message){toast(result.message);return;}if(action==='reset'||(action==='escape'&&!scene.selected)){selected=null;tab='Overview';renderTree();renderInspector();$('#breadcrumbs').innerHTML=`${icon('home')}<span>MTR DRILLTECH</span><span class="crumb-sep">/</span><b>${modelInfo[mode].short} overview</b>`;}if(action==='reset'||action==='unhide')$$('.sys-chip').forEach(b=>b.classList.remove('off'));if(action==='connections')renderInspector();if(action==='hide')toast('Equipment hidden. Press U to show all.');syncButtons();}
$$('[data-action]').forEach(b=>b.onclick=()=>runAction(b.dataset.action));$$('[data-camera]').forEach(b=>b.onclick=()=>{scene?.setView(b.dataset.camera);$$('[data-camera]').forEach(p=>p.classList.toggle('active',p===b));});
function togglePanel(side,show){const w=$('#workspace');if(side==='left'){if(innerWidth<=650)w.classList.toggle('mobile-navigator',show);else w.classList.toggle('left-collapsed',!show);}else{if(innerWidth<=900){w.classList.toggle('mobile-inspector',show);w.classList.remove('right-collapsed');}else w.classList.toggle('right-collapsed',!show);}updatePanelButtons();}
function updatePanelButtons(){$('#toggle-left').hidden=!(innerWidth<=650||$('#workspace').classList.contains('left-collapsed')||$('#workspace').classList.contains('expanded'));$('#toggle-right').hidden=!(innerWidth<=900||$('#workspace').classList.contains('right-collapsed')||$('#workspace').classList.contains('expanded'));}
$('#collapse-left').onclick=()=>togglePanel('left',false);$('#toggle-left').onclick=()=>{$('#workspace').classList.remove('expanded');togglePanel('left',true);};$('#collapse-right').onclick=()=>togglePanel('right',false);$('#toggle-right').onclick=()=>{$('#workspace').classList.remove('expanded');togglePanel('right',true);};$('#fullscreen').onclick=()=>{$('#workspace').classList.toggle('expanded');updatePanelButtons();};addEventListener('resize',updatePanelButtons);updatePanelButtons();
$('#help').onclick=()=>dialog('Explore the rig',`<p>Use the mouse, touch, or the equipment navigator.</p>${[['Orbit','Drag'],['Pan','Right-drag'],['Zoom','Scroll / pinch'],['Frame equipment','Double-click'],['Focus selected','F'],['Isolate selected','I'],['Fade other equipment','D'],['Hide / show all','H / U'],['View internals','X'],['Reveal seabed','G'],['Reset scene','R'],['Step out / clear','Esc'],['Search equipment','/']].map(([name,key])=>`<div class="shortcut-row"><span>${name}</span><kbd>${key}</kbd></div>`).join('')}<p>Select an assembly to see whether authored internal parts are available. The HWU model includes hydraulic jack, slip and pressure-control details.</p>`);
addEventListener('keydown',e=>{if($('#dialog').open)return;if(['INPUT','TEXTAREA','SELECT'].includes(document.activeElement?.tagName)||document.activeElement?.isContentEditable)return;if(e.ctrlKey||e.metaKey||e.altKey||view!=='explorer')return;const map={f:'focus',i:'isolate',d:'fade',h:'hide',u:'unhide',x:'internals',g:'subsurface',r:'reset',Escape:'escape'};const action=map[e.key]||map[e.key.toLowerCase()];if(action){e.preventDefault();runAction(action);}if(e.key==='/'){e.preventDefault();togglePanel('left',true);$('#search').focus();}});
$('#integrity').onclick=()=>{const m=scene?.integrity();const ids=components.map(c=>c.id);const broken=components.flatMap(c=>c.connections.filter(id=>!byId.has(id)));const rows=m?[['Component records',components.length],['Modelled assemblies',m.modelled],['Renderable mesh groups',m.meshes],['Unique component IDs',new Set(ids).size],['Unmatched geometry records',m.unmatched.length],['Broken connections',broken.length],['LTDS rig records',catalog.rigs.filter(r=>r.recordKind==='rig_record').length],['HWU unit records',hwuCatalog.rigs.filter(r=>r.recordKind==='unit_record').length],['Planning placeholders',catalog.rigs.filter(r=>r.recordKind==='planning_placeholder').length+hwuCatalog.rigs.filter(r=>r.recordKind==='planning_placeholder').length],['Geometry status','Illustrative']]:[['3D scene','Unavailable']];dialog('Model integrity',`<p>Checks are calculated from the loaded equipment and fleet records.</p>${rows.map(([a,b])=>`<div class="spec-row"><span>${esc(a)}</span><b>${esc(b)}</b></div>`).join('')}<p>Model coverage describes this illustrative explorer, not a complete inventory of a specific vessel.</p>`);};

async function selectRig(id){
  const record=[...catalog.rigs,...hwuCatalog.rigs].find(r=>r.id===id);
  if(!record||!isUnitRecord(record))throw Error('Unknown assigned unit record');
  const next=record.assetType==='hydraulic_workover_unit'?'hwu':'jackup';
  if(mode!==next)await switchModel(next);
  selectedRig=record;fleetAsset=next;fleetFilter=record.recordKind;
  $('#scene-title').textContent=record.name;$('#rig-selector-label').textContent=record.name;
  setView('explorer');runAction('reset');toast(`${record.name} · generic model; actual unit layout is not verified.`);
}
function fleetCards(){
  const records=filteredFleet();
  $('#fleet-grid').innerHTML=records.map(r=>`<article class="rig-card"><div class="rig-card-top"><span class="rig-icon">${icon(fleetAsset==='hwu'?'hoist':'rig')}</span><span class="badge ${isUnitRecord(r)?'':'amber'}">${isUnitRecord(r)?'LTDS listed':'Planning slot'}</span></div><h2>${esc(r.name)}</h2><div class="region-tags">${r.sourceRegionCodes.map(code=>`<span title="Code printed in planning source; not current position">${esc(code)}</span>`).join('')}${r.hphtExplicit?'<span>HPHT label in source</span>':''}</div><p>${esc(r.note)}</p><p class="source-label">${esc(r.sourceLabel)}<br>June 2026 · Page ${r.sourcePage}</p>${isUnitRecord(r)?`<button class="card-button" data-rig="${r.id}">Explore illustrative ${fleetAsset==='hwu'?'HWU':'model'} ${icon('arrow')}</button>`:'<div class="card-button">No unit assigned</div>'}</article>`).join('')||'<div class="empty-search">No matching planning records.</div>';
  $$('[data-rig]').forEach(b=>b.onclick=()=>selectRig(b.dataset.rig));
}
function renderFleet(){
  const data=currentFleet();if(!data)return;const hwu=fleetAsset==='hwu';
  $('#fleet-view').innerHTML=`<div class="fleet-heading"><div><span class="eyebrow">MALAYSIA · LTDS PLANNING RECORDS</span><h1>${hwu?'Hydraulic workover units':'Meet the jack-up fleet'}</h1><p>June 2026 LTDS records with neutral labels. Real unit names are withheld. ${hwu?'Identified unit records and unassigned planning slots are counted separately.':'Explore the planning context and illustrative 3D rig.'}</p></div><div class="fleet-stats"><div><strong>${data.rigs.filter(isUnitRecord).length}</strong><small>${hwu?'Unit':'Rig'} records</small></div><div><strong>${data.rigs.filter(r=>r.recordKind==='planning_placeholder').length}</strong><small>Planning slots</small></div></div></div><div class="fleet-asset-tabs" aria-label="Planning asset type"><button class="filter-btn ${!hwu?'active':''}" data-fleet-asset="jackup">${icon('rig')} Jack-up rigs</button><button class="filter-btn ${hwu?'active':''}" data-fleet-asset="hwu">${icon('hoist')} Hydraulic workover units</button></div><div class="fleet-tools"><div class="fleet-search">${icon('search')}<input id="fleet-search" type="search" placeholder="Search ${hwu?'HWU':'fleet'} records…" aria-label="Search fleet records" value="${esc(fleetQuery)}"/></div><button class="filter-btn ${fleetFilter===recordKind()?'active':''}" data-filter="${recordKind()}">${hwu?'Unit':'Rig'} records</button><button class="filter-btn ${fleetFilter==='planning_placeholder'?'active':''}" data-filter="planning_placeholder">Planning slots</button><button class="filter-btn" id="planning-outlook">Planning outlook</button><button class="filter-btn" id="export-fleet">${icon('download')} CSV</button></div><div class="fleet-grid" id="fleet-grid"></div><div class="fleet-note"><b>Planning snapshot, not live operations.</b> Neutral labels do not imply MTR ownership. LTDS records do not establish current location, availability or completed activity. ${hwu?'The source lists HWU rows within its Work Over section and marks HWU P&A plans as Notional. Placeholders are unassigned capacity, not verified additional units.':'Overseas planning context and placeholders are preserved.'} Generic 3D geometry is not a verified model of any specific unit. Source: June 2026 LTDS, page 1, ${hwu?'Work Over · HWU':'JACK-UP'} · Confidential / Internal.</div>`;
  fleetCards();$('#fleet-search').oninput=e=>{fleetQuery=e.target.value;fleetCards();};
  $$('[data-filter]').forEach(b=>b.onclick=()=>{fleetFilter=b.dataset.filter;renderFleet();});
  $$('[data-fleet-asset]').forEach(b=>b.onclick=()=>{fleetAsset=b.dataset.fleetAsset;fleetFilter=recordKind();fleetQuery='';renderFleet();});
  $('#planning-outlook').onclick=planningOutlook;$('#export-fleet').onclick=exportFleet;
}
function planningOutlook(){
  const source=currentFleet(),hwu=fleetAsset==='hwu';const data=hwu?source.monthlyHWUTotalsAsPrinted:source.monthlyJackUpTotalsAsPrinted;
  if(!data){dialog('HWU planning outlook','<p>The source contains HWU planning records, but a verified monthly total series is not included. No dates or utilization are inferred.</p>');return;}
  const scope=source.monthlyTotalsScopeAsPrinted||'HWU TOTAL';
  dialog(`${hwu?'HWU':'Jack-up'} planning outlook`,`<p>Monthly ${hwu?'HWU':'jack-up'} totals transcribed from the June 2026 source. Printed scope: <b>${esc(scope)}</b>.</p><p>These are schedule totals, not distinct assigned units or confirmed current operations. ${hwu?'HWU P&amp;A campaign plans are marked Notional in the source.':''}</p>${[2026,2027,2028,2029].filter(year=>Array.isArray(data[year])).map(year=>`<h3>${year}${year>2026?' · For indication only':''}</h3><div class="planning-bars">${data[year].map((n,i)=>`<div><span>${n===null?'—':n}</span><div style="height:${n===null?0:n*6}px;background:${year===2026?'#20967a':'#2c6269'}"></div><small>${['J','F','M','A','M','J','J','A','S','O','N','D'][i]}</small></div>`).join('')}</div>`).join('')}<p>${esc(source.monthlyTotalsCaution||'Source totals are not current utilization.')}</p><p>2030 is clipped in the supplied document and is not extrapolated. Campaign dates and blank-period availability have not been inferred.</p>`);
}
function exportFleet(){
  const header=['display_label','asset_type','record_kind','source_reference','snapshot','source_page','current_status','note'];
  const csv=[header,...filteredFleet().map(r=>[r.name,r.assetType,r.recordKind,r.sourceLabel,'2026-06',r.sourcePage,'not_verified',r.note])].map(row=>row.map(v=>'"'+String(v).replaceAll('"','""')+'"').join(',')).join('\r\n');
  const url=URL.createObjectURL(new Blob(['\uFEFF'+csv],{type:'text/csv;charset=utf-8'}));const a=document.createElement('a');a.href=url;a.download=`MTR-${fleetAsset==='hwu'?'HWU':'LTDS'}-June-2026-planning.csv`;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);
}
async function startScene(){
  $('#canvas-loading').hidden=false;
  try{const {createScene}=await import('./scene.js');scene=createScene($('#viewport'),components,select,hover=>{const label=$('#hover-label');if(!hover){label.hidden=true;return;}label.textContent=hover.name;label.hidden=false;label.style.left=Math.min(hover.x+14,$('#viewport').clientWidth-180)+'px';label.style.top=Math.max(10,hover.y-36)+'px';},mode);$('#canvas-loading').hidden=true;$('#integrity-count').textContent=`${scene.integrity().modelled} linked`;$$('[data-system-toggle]').forEach(b=>b.onclick=()=>{b.classList.toggle('off',!scene.toggleSystem(b.dataset.systemToggle));});syncButtons();}
  catch(error){console.error('3D scene unavailable',error);$('#canvas-loading').innerHTML=`<div class="error-box"><h2>3D view unavailable</h2><p>This browser could not initialize the model. Equipment descriptions and Malaysia fleet records are still available.</p><button class="primary-btn" id="retry-scene">Try again</button></div>`;$('#retry-scene').onclick=()=>location.reload();}
}

async function switchModel(next){
  if(!modelInfo[next])throw Error('Unknown rig model');
  if(next===mode&&scene)return;
  scene?.dispose();scene=null;mode=next;selected=null;selectedRig=null;tab='Overview';
  const info=modelInfo[mode];
  const landIds=new Set(['derrick','crown','travelling','top-drive','drawworks','drill-string','bop','choke','mud-pumps','mud-tanks','shakers','generators','pipe-rack','drill-floor']);
  components=mode==='hwu'?hwuEquipment.components:mode==='land'?allComponents.filter(c=>landIds.has(c.id)).map(c=>({...c,connections:c.connections.filter(id=>landIds.has(id))})):allComponents;
  sourceNote=mode==='hwu'?hwuEquipment.sourceNote:rigEquipment.sourceNote;
  if(mode==='land')components=components.map(c=>{if(c.id==='generators')return {...c,location:'Ground-level power and electrical skids'};if(c.id==='mud-tanks')return {...c,location:'Ground-level mud processing modules'};if(c.id==='pipe-rack')return {...c,location:'Ground-level tubular storage and inclined catwalk',engineering:'The pipe rack supports tubular storage beside the rig. A catwalk and inclined ramp connect storage with the elevated drill floor. The model illustrates that handling relationship without defining load limits, an operating sequence or a verified handling system.',hazards:['Stored tubular movement','Suspended loads','Pinch points']};return c;});
  byId.clear();components.forEach(c=>byId.set(c.id,c));systems=allSystems.filter(s=>components.some(c=>c.system===s.id)).map(s=>mode==='hwu'&&s.id==='PS'?{...s,name:'Hydraulic Power & Control'}:mode==='hwu'&&s.id==='DS'?{...s,name:'Tubulars & Handling'}:mode==='hwu'&&s.id==='SS'?{...s,name:'Structure & Access'}:s);
  $('.stage').dataset.model=mode;$('#search').value='';$$('[data-model]').forEach(b=>b.classList.toggle('active',b.dataset.model===mode));
  $('#scene-title').textContent=info.title;$('.scene-title .eyebrow').textContent=info.eyebrow;
  $('.rig-summary b').textContent=info.overview;$('#component-count').textContent=`${systems.length} systems · ${components.length} assemblies`;
  $('#rig-selector-label').textContent=mode==='hwu'?'Select an HWU record':'Select a Malaysia rig';
  fleetAsset=mode==='hwu'?'hwu':'jackup';fleetFilter=recordKind();fleetQuery='';
  $('#viewport').setAttribute('aria-label',`Interactive 3D ${info.overview}. Drag to orbit, scroll to zoom. Equipment is also available in the navigator.`);
  $('[data-action="subsurface"] span:not([data-icon])').textContent=mode==='land'?'Below':'Subsea';
  $('#system-chips').innerHTML=systems.map(s=>`<button class="sys-chip" data-system-toggle="${s.id}" title="Show or hide ${s.name}" aria-label="Toggle ${s.name} visibility">${s.id}</button>`).join('');
  $('#breadcrumbs').innerHTML=`${icon('home')}<span>MTR DRILLTECH</span><span class="crumb-sep">/</span><b>${info.short} overview</b>`;
  renderTree();renderInspector();await startScene();
}
$$('[data-model]').forEach(b=>b.onclick=()=>switchModel(b.dataset.model));


const EMPTY_CATALOG={rigs:[],datasetName:'Withheld from the public edition',sourceSnapshotMonth:null,coverage:null,caveats:[],rigNamesRemoved:true};
try{
  const readData=path=>fetch(path).then(r=>{if(!r.ok)throw Error('Data unavailable: '+path);return r.json();});
  const [equipmentData,ltds,hwuData,hwuRecords]=await Promise.all([readData('data/equipment.json'),Promise.resolve(EMPTY_CATALOG),readData('data/hwu-equipment.json'),Promise.resolve(EMPTY_CATALOG)]);
  rigEquipment=equipmentData;allComponents=equipmentData.components;catalog=ltds;hwuEquipment=hwuData;hwuCatalog=hwuRecords;
  await switchModel('jackup');
  $('.main-nav .count').textContent=[...catalog.rigs,...hwuCatalog.rigs].filter(isUnitRecord).length;$('#rig-selector-label').textContent='Illustrative models only';
}catch(error){console.error(error);$('#canvas-loading').innerHTML='<div class="error-box"><h2>Could not load the explorer</h2><p>Please refresh the local preview to try loading the equipment records again.</p></div>';}

// Structured browser tools share the visible interface actions and state.
export const explorer={
  getState:()=>({view,selectedComponent:selected,selectedRig:selectedRig?.name||null,model:'illustrative-generic-'+mode,scene:scene?.state||null}),
  setModel:async next=>{if(!modelInfo[next])throw Error('Unknown rig model');await switchModel(next);setView('explorer');return explorer.getState();},
  getComponents:()=>components.map(({id,name,system})=>({id,name,system})),
  selectComponent:id=>{if(!byId.has(id))throw Error('Unknown component');setView('explorer');select(id,true);return explorer.getState();},
  setCamera:camera=>{if(!['iso','top','deck'].includes(camera))throw Error('Unknown camera view');scene?.setView(camera);return{camera};},
  sceneAction:action=>{if(!['focus','isolate','fade','internals','subsurface','reset','unhide'].includes(action))throw Error('Unsupported action');runAction(action);return explorer.getState();},
  getPlanningRecords:assetType=>{if(!['jackup','hwu'].includes(assetType))throw Error('Unknown asset type');const data=assetType==='hwu'?hwuCatalog:catalog;return{assetType,sourceSnapshotMonth:data.sourceSnapshotMonth,units:data.rigs.filter(isUnitRecord).length,placeholders:data.rigs.filter(r=>r.recordKind==='planning_placeholder').length,records:data.rigs.map(({id,name,recordKind,sourcePage})=>({id,name,recordKind,sourcePage}))};},
  openPlanningRecord:async id=>{await selectRig(id);return explorer.getState();}
};
